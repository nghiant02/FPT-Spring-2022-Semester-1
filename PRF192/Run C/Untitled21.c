#include<stdio.h>
int main ()
      Accept num1, op, num2 ? "%If%c%If"
      switch (op)
        { case t': result = num1 + num2;
                print out result;
                break;
         case : result = num1 - num2;
                print out result;
                break;
         case ** : result = num1 * num2;
                print out result;
                break;
         case /' : if ( num2=D0)
                  print out "Divide by 0"
                else
                 { result = num1/num2;
                   print out result;
               }
                break;
         default: print out "Op is not supported"
		}
